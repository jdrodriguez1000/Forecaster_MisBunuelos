---
description: Automatiza la creación del notebook `01_data_discovery.ipynb` mediante un script generador estandarizado.
---

# Workflow: Fase 1 - Creación del Notebook de Discovery (Data Extraction)

EXTREMELY IMPORTANT: DO NOT EDIT THIS FILE DIRECTLY. EDIT THE SCRIPT scripts/gen_discoverer.py INSTEAD.
Este flujo de trabajo tiene como objetivo generar automáticamente el notebook `notebooks/01_data_discovery.ipynb` mediante un script generador estandarizado.

## 🛠️ Pasos de Ejecución

### Paso 1: Generación del Notebook
Ejecuta el script generador que crea el notebook con la lógica encapsulada.

// turbo
python scripts/gen_discoverer.py

### Paso 2: Validación y Ejecución
El notebook generado en `notebooks/01_data_discovery.ipynb` debe ser ejecutado para realizar la extracción y análisis.


### Paso 3: Ejecución Manual y Verificación
* **Acción:** El usuario abrirá y ejecutará manualmente el notebook `notebooks/01_data_discovery.ipynb`.

### Paso 4: Limpieza de Archivos Temporales
* **Acción:** Eliminar cualquier archivo temporal generado durante la ejecución (`.py`, `.txt`, `.log`) que no forme parte del repositorio.
* **Comando Sugerido:** `Remove-Item -Path "notebooks/run_*.py", "notebooks/*.log", "notebooks/*.txt" -ErrorAction SilentlyContinue`
* **Motivo:** Mantener el entorno de notebooks limpio y evitar commits de scripts de ejecución temporal.
